// Re-export from the auth context for convenient hook-style imports.
// You can import useAuth from either "@/hooks/useAuth" or "@/context/auth".
export { useAuth, AuthProvider } from "@/context/auth";
export type { AuthUser } from "@/context/auth";
