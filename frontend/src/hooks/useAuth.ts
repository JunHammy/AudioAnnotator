// Auth state lives in Providers (AuthContext) — shared across the entire app.
// Import useAuth from there directly, or use this re-export for convenience.
export { useAuth } from "@/components/providers";
export type { AuthUser } from "@/components/providers";
