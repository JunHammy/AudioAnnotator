# Emotion finalization has been removed.
# Each annotator's emotion tags are stored as a JSON list directly on their
# SpeakerSegment copy (source="annotator").  No majority voting or tier
# resolution is performed — the export aggregates per-annotator lists as-is.
